# Every "%…%" fragment in here has to survive verbatim: the loader is called
# with PROCESS_IMPORTS only, so placeholder substitution - which would replace
# "%identifier%" with the identifier below - never runs.
identifier: placeholders
title: 'Save 50% today, 100% sure'
description: '%identifier% is not substituted'

scenarios:
  - '%identifier%.yaml'
